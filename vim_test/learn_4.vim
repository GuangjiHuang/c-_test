" # conditions
"
"# if /esleif /else -> endif
 
let number = -1

if number > 0
    echo "the positive value"

elseif number == 0
    echo "the value is the zero"

else
    echo "the negative value"

endif
