"# double quote and the single quote
"
"double quote ->: the string, can be more than one line
"just like the pyton's doulbe quote


"single quote ->: the string, just can be the one line, note that, the
"backward slash is not the escape character, the single quote is the only
"character need to be escaped, double single quote to escape the single quote
"mark
