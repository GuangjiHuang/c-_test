"# increment/decrement & ternary operator ( just like the
"-> condition ? expression_1 : expression_2
"
"
"that is the same as the -/ %/ / * and the other operator

let number = 1
let number += 1

let number += 1

echo number

" the ternary operator
let string_result = number > 0 ? "positvie" : "negative"
echo string_result
