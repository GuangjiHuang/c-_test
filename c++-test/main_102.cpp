#include<iostream>
#include<cmath>
using namespace std;

int main()
{
    ;
    // test the floor function and the round funciton
    int var_a = floor(3.5);
    int var_b = round(3.5);
    cout << "the value of the var_a is the: " << var_a << endl;
    cout << "the value of the var_b is the: " << var_b << endl;
    //cout << "the value of the var_c is the: " << var_c << endl;

    return 0;
}
