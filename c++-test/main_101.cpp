#include<iostream>
using namespace std;

int main()
{
    cout << "the size of the short int is the: " << sizeof(short int) << endl;
    cout << "the size of the int is the: " << sizeof(int) << endl;
    cout << "the size of the long is the: " << sizeof(long) << endl;
    cout << "the size of the long long int is the: " << sizeof(long long) << endl;


    return 0;
}
